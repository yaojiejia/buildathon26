# Slack app (create at https://api.slack.com/apps)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_CHANNEL_ID=C0123456789
SLACK_SIGNING_SECRET=your-signing-secret

# Optional: custom URL for "Open in Cursor" button (default: cursor.com/open?url=repoLink)
# CURSOR_OPEN_URL=https://cursor.com/open?url=

# Debug 401 on /api/slack/interactions: set to 1 and check server logs for [Slack signature] failed
# DEBUG_SLACK_SIGNATURE=1

# Dev only: skip signature verification so you can test buttons (remove in production)
# SKIP_SLACK_SIGNATURE_VERIFY=1

# GitHub App / webhook (TICKET-1.1)
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# Slack-initiated cases (TICKET-1.3): default repo for #123 style refs and "Create GitHub issue" link
# GITHUB_DEFAULT_REPO=owner/repo

# SQLite for case records
DATABASE_URL="file:./dev.db"
